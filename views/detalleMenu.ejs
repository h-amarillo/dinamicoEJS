<!DOCTYPE html>
<html>
<!-- head -->
  <%- include('./headDetail') %>
  <body>
<!-- Navbar -->
<%- include('./navbar') %>
    <!-- Header -->
<%- include('./headerDetail') %>
    <!-- Page content -->
    <div class="ps-content">

      <!-- Menu Section -->
      <div class="ps-row ps-padding-64" id="menu">
        <div class="ps-col l6 ps-padding-large">

          <h1 class="ps-center"><a href="/detalle/<%= platillo.id %>"></a> <%= platillo.titulo %></h1>
          <br />
          <% if (platillo.descripcionDetallada == "") { %>
            <h4>"Platillo Típico"</h4>
          <% } else{ %>
            <h4><%= platillo.descripcionDetallada %></h4>
          <% } %>   
          <p class="ps-text-grey">
            <%= platillo.precio %>
          </p>
        </div>

        <div class="ps-col l6 ps-padding-large ps-img-menu">
          <img
            src="/images/<%= platillo.img %>"
            class="ps-round ps-image ps-opacity-min"
            alt="platillo"
          />
        </div>
      </div>

      <!-- Footer -->
      <footer class="ps-center ps-light-grey ps-padding-32">
        <p>Powered by Pimienta & Sal</p>
      </footer>
    </div>
  </body>
</html>
